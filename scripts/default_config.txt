### CONFIGURATION FILE ###
##LOAD MAIN VARIABLES##
# DATA #
installation_path=''
case='' ## ID ##
root_dir='' ## Parent directory of the genomes ##
tumor_genome=''
control_genome=''
rois_list=${installation_path}/'lib/ROIs' ## List of regions on interest (BED-formatted) ##
armadillo_data="${installation_path}'lib/armadillo_data/" ##Output dir of prepare_data script ##
repeatsDB=${installation_path}/'lib/hg19_repeatmasker_DB_sorted.bed.gz' ## Repeat annotation database ##

# THRESHOLDS AND PARAMETERS #
tum_qual=30
gc_content=80 ## Max. %GC allowed in reads ##
max_errors=2
mincov=80
control_cutoff=3
control_contamination=15 ##(%)
read_length=151
threads=3
tumor_cutoff=4
control_qual=0
mapq=40 ## Minimum mapq after realignment ##
port='9001' ## Port previously opened for gfClient (Blat) ##
skip="false" ## Skip the miniBAM alignment. Only use it to reanalyse with new parameters.
print='TRUE' ## Print a log file of the candidates lost during the analysis ##
